apiVersion: v1
kind: Service
metadata:
  name: {{ include "trade-settlement.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "trade-settlement.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default 8080 }}
      protocol: TCP
      name: http
  selector:
    {{- include "trade-settlement.selectorLabels" . | nindent 4 }}
